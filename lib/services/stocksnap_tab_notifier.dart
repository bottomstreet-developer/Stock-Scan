import 'package:flutter/foundation.dart';

/// Bottom navigation index for [HomeScreen] (0 = Inventory, 1 = Dashboard, 2 = Settings).
final ValueNotifier<int> stocksnapTabNotifier = ValueNotifier<int>(0);
